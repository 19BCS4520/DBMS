//DML stands for Data Manipulation Language. It is used for accessing and manipulating data in a database. It handles user requests.
Here are some tasks that come under DML-:
//insert data
create table student(id number,name varchar(20),age varchar(20));
alter table student add phone number(10);
alter table student drop column phone;
alter table student modify id varchar(20);
alter table student rename column id to stud_id;
insert into student values(4520,'Parikshit',19);
insert into student values(4521,'avisha',17);
insert into student values (4522,'anita',23);
update student set age=18 where name='anita';
select *from student;
